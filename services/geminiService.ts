// This service has been disabled as part of the removal of AI features.
export const evaluateCase = async (userQuery: string): Promise<string> => {
  return "AI features are currently disabled.";
};
